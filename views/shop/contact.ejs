<%- include('../includes/userHead.ejs') %>
<%- include('../includes/userNavbar.ejs') %>

<style>
  

    h2 {
        color: #333;
        margin-top: 200px;
        text-align: center;
        margin-bottom: 25px;
    }

    p {
        line-height: 1.5;
        color: #666;
        text-align: center;
       
    }

    form {
        max-width: 600px;
        margin: 20px auto;
    }

    label {
        display: block;
        margin-bottom: 8px;
    }

    input, textarea {
        width: 100%;
        padding: 8px;
        margin-bottom: 16px;
        box-sizing: border-box;
    }

    button {
        background-color: #4CAF50;
        color: white;
        padding: 10px 15px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }
</style>

   
<h2>Submit Your Ideas</h2>

<p><b>We value your ideas and feedback! Share your thoughts with us using the form below:</b></p>
<div id="error-message" class="error-message"></div>

<form action="/contactsubmit" method="post"  onsubmit="return validateForm()">


   
    <label for="email">Your Email:</label>
    <input type="email" id="email" name="email" required>

    
    <label for="phone">Phone Number</label>
    <input type="text" id="phone" name="phone" required>

    <label for="idea">Your Idea:</label>
    <textarea id="idea" name="idea" rows="4" required></textarea>


    <button type="submit">Submit Idea</button>
</form>

<p><b>Thank you for contributing to StyleSphere ! We appreciate your input.</b></p>

<script>
    function validateForm() {
      
        var email = document.getElementById("email").value;
        var phone = document.getElementById("phone").value;
        var idea = document.getElementById("idea").value;
        var errorMessage = document.getElementById("error-message");

        // Reset error message
        errorMessage.innerHTML = "";

      

        // Validate email format
        var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!emailRegex.test(email)) {
            errorMessage.innerHTML = "Invalid email format";
            return false;
        }

        if (isNaN(phone)) {
            errorMessage.innerHTML = "Phone number must be numeric";
            return false;
        }

      
        showThankYouAlert();
        return true;
    }
    function showThankYouAlert() {
        Swal.fire({
            icon: 'success',
            title: 'Thank You!',
            text: 'Your idea has been submitted successfully!',
        }).then(()=>{
            window.location='/';
        });
    }
</script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

<%- include('../includes/userFooter.ejs') %>
<%- include('../includes/end.ejs') %>